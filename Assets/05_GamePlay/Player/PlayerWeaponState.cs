using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerWeaponState : MonoBehaviour
{
    /*private int hitCount = 0;

    public void TreeHit()
    {
        hitCount++;

        if (hitCount == 1)
        {
            GamePlay.Instance.natureManager.FirstHit();
        }
        else if (hitCount == 2)
        {
            hitCount = 0;
            GamePlay.Instance.natureManager.LastHit();
        }
        else
        {
            Debug.Log("카운트 버그");
        }
    }*/
}
